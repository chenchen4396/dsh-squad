# DSH ships its packages on prerelease ranges (0.1.x-rc.N), and its own
# packages peer-require each other at pinned prerelease versions that npm
# cannot reconcile while this plugin builds against the newest ones.
# Install-time peer validation is therefore relaxed for this project.
legacy-peer-deps=true
